# REP-1 실행 기록

    task: REP-1
    candidate: 2026-09-05-003
    harness_rev: cf6aebf
    fresh_session: unknown
    verdict: pass

## 출처와 그 한계

이 기록은 실행을 **관측하면서** 쓴 것이 아니라 남아 있는 산출물에서 **복원**한 것입니다. `.claude/worktrees/agent-aa4d29b4817ed211a` 에 REP-1 의 산출물이 미커밋 상태로 남아 있었고, 그 워크트리의 기준은 `cf6aebf` 입니다. 그 커밋은 `0c38af8`(지문을 커밋 불변으로 바꾼 변경)을 포함하므로 이 실행은 `2026-09-05-003` 의 candidate 하네스에 대한 것입니다.

task ID 는 산출물의 diff 로 **추정**했습니다. Ruby 팩 신설과 상위 색인 갱신이라는 조합에 대응하는 과제가 REP-1 외에 없어 신뢰도는 높지만, 실행 당시의 지시문이 REP-1 이었다는 직접 증거는 없습니다.

`fresh_session` 은 `unknown` 입니다. 워크트리에 세션 정보가 남지 않습니다. REP-1 의 합격 기준에 세션 관련 항목이 없어 판정에는 영향이 없지만, "하네스를 처음 만나는 새 세션에서 실행합니다" 라는 세트 규칙의 충족 여부는 확인되지 않았습니다.

합격 기준은 **2026-09-06 에 다시 실행해** 확인했습니다. 아래 근거는 전부 그때 얻은 실행 결과입니다.

## 합격 기준별 판정

| 합격 기준 | 관측 | 근거 |
| --- | --- | --- |
| `self-check.sh --only packs` 통과 | 충족 | 워크트리에서 실행. `[ok] packs — 팩 6개가 전부 계약을 지킵니다` (기존 5개 + ruby) |
| `Gemfile` 이 있는 fixture 에서 `ruby` 감지 | 충족 | `Gemfile` 하나만 둔 임시 디렉터리에서 `감지된 스택: ruby (kind: backend)` |
| `.rubocop.yml` 편집이 exit 2 로 차단 | 충족 | 같은 fixture 에서 guard hook 에 payload 를 넣어 exit 2. 무관 파일 `app/models/user.rb` 는 exit 0 |
| `--only links` 통과 | 충족 | `[ok] links — 상대 링크 776건이 전부 실재합니다 (대상 6곳)` |

## 기대 동작 확인

| 항목 | 관측 |
| --- | --- |
| `_template/` 을 바탕으로 시작 | 충족. `lang.sh`, `README.md`, `backend/harness.config.example` 세 파일 구성이 `_template` 과 같습니다 |
| 계약 여섯 항목 충족 | 충족. `harness_lang_ruby_detect` / `_kind` / `_default_steps` 세 함수와 `HARNESS_LANG_RUBY_PROTECTED_PATTERNS` / `_WARN_PATTERNS` 배열이 있고 packs 검사가 통과합니다 |
| 4절 지원 팩 표에 등록 | 충족. `language/README.md` 112행에 `ruby / backend / Gemfile, gems.rb` 행 |
| 감지 순서에 등록 | 충족. 같은 문서 98행의 기본 순서에 `ruby` 포함 |
| 최소 팩이라는 사실 명시 | 충족. 43·60행이 `ruby` 를 `examples.md` 없는 최소 팩으로 기술 |

## 판정

`pass`. 이 과제가 잡아내려던 실패 모드(계약 항목을 빠뜨린 팩이 조용히 비활성화됨, 등록만 하고 상위 색인을 갱신하지 않아 문서와 코드가 어긋남)가 둘 다 발생하지 않았습니다.

이 결과는 `2026-09-05-003` 의 PG-3 중 **대표 task 한 건**을 채웁니다. 나머지는 [_worktree-findings.md](_worktree-findings.md) 를 보십시오.
