# 예시 항목입니다. 실제 로그가 아니므로 파일명에 .example 이 붙어 있고 validate 대상이 아닙니다.
# 실제 항목은 프로젝트 루트의 improvement-log/2026-08-09-001.yaml 처럼 .example 없이 저장합니다.
# 키별 명세는 schema.md, 승격 판정은 ../rules/promotion-gate.rule.md 를 따릅니다.
# 이 예시는 언어 중립입니다. 같은 사건을 실제 스택의 클래스·도구 이름으로 채운 판은
# ../language/<언어>/<kind>/improvement-log.example.yaml (java/backend, typescript/frontend, typescript/backend, python/backend) 에 있습니다.
id: 2026-08-09-001
date: 2026-08-09
status: candidate
symptom: Controller 계층이 Repository 구현체에 직접 의존했고 아키텍처 규칙 검사가 실패했습니다.
evidence: |
  .harness/verify.json 의 arch-test 단계 status=fail, exit_code=1.
  실패 규칙: controllers-must-not-depend-on-repositories
  실패 메시지: controller 모듈이 repository 모듈을 직접 참조합니다 (허용 경로 미표시)
  로그: .harness/logs/arch-test.log:42
  재현: harness/scripts/verify.sh --only arch-test
root_cause: 계층 의존 규칙 문서가 진입점 문서에서 안내되지 않아 에이전트의 탐색 경로에 들어오지 않았고, 아키텍처 규칙의 실패 메시지도 허용된 호출 경로를 알려주지 않았습니다.
fix: Controller 가 UseCase 계층을 경유하도록 의존성을 변경했습니다.
recurrence_risk: high
harness_element: HE-4
proposed_harness_change: 아키텍처 문서의 발견 경로를 보강하고, 아키텍처 규칙의 실패 메시지에 허용된 호출 경로(controller → use-case → repository)를 포함시킵니다.
preferred_enforcement: arch-rule
trust: untrusted
regression_check: |
  대상: REP-2, REP-5, HLD-1, HLD-3
  기준: 총점이 기준선 이상이고 correctness·architecture·quality·behavior·performance·subjective 어느 계층도 기준선보다 낮지 않을 것.
        추가로 규칙 적용 후 정당한 구현(use-case 경유 호출)이 차단되지 않을 것.
  근거 파일: .harness/baseline-eval.json, .harness/latest-eval.json
owner: unassigned
expires: 2026-11-09
