# Java(Spring/ArchUnit) 스택의 improvement candidate 예시입니다.
# 코어 예시 harness/improvement-log/2026-08-09-001.example.yaml 의 언어 중립 문장을 이 스택의 실제 클래스·도구 이름으로 옮긴 판입니다.
# 실제 항목은 프로젝트 루트의 improvement-log/<id>.yaml 로 저장하며, 키 순서는 harness/improvement-log/schema.md 를 따릅니다.
id: 2026-08-09-002
date: 2026-08-09
status: candidate
symptom: MemberController 가 MemberJpaRepository 에 직접 의존했고 ArchUnit 테스트가 실패했습니다.
evidence: |
  .harness/verify.json 의 arch-test 단계 status=fail, exit_code=1.
  실패 테스트: ArchitectureTest.controllersMustNotDependOnRepositories
  실패 메시지: Constructor <com.example.member.MemberController.<init>(MemberJpaRepository)> calls constructor <MemberJpaRepository>
  로그: .harness/logs/arch-test.log:42
  재현: harness/scripts/verify.sh --only arch-test
root_cause: 계층 의존 규칙 문서(docs/architecture/layers.md)가 AGENTS.md 에서 안내되지 않아 에이전트의 탐색 경로에 들어오지 않았고, ArchUnit 규칙의 실패 메시지도 허용된 호출 경로(controller → use-case → repository)를 알려주지 않았습니다.
fix: MemberController 가 MemberUseCase 를 경유하도록 생성자 의존성을 변경했습니다.
recurrence_risk: high
harness_element: HE-4
proposed_harness_change: AGENTS.md 의 Architecture 절에서 docs/architecture/layers.md 를 링크하고, ArchUnit 규칙에 .because("controller 는 use-case 를 경유합니다. 허용 경로: controller → usecase → repository") 를 붙여 실패 메시지가 대안을 알려주게 합니다.
preferred_enforcement: arch-rule
trust: untrusted
regression_check: |
  대상: REP-2, REP-5, HLD-1, HLD-3
  기준: 코어 예시(harness/improvement-log/2026-08-09-001.example.yaml)와 같습니다. 총점과 6계층 모두 기준선 이상.
  이 스택의 추가 확인: 정당한 구현(use-case 경유 호출, @Transactional 서비스 경유 호출)이 차단되지 않을 것.
owner: unassigned
expires: 2026-11-09
